from setuptools import setup, find_packages

setup(name='spark-python-scala-udf',
      version='0.0.1',
      description='Python and Scala UDF sample app',
      author='andre',
      packages = find_packages(),
      zip_safe=False)
